<p id="notice"><%= notice %></p>

<h1>
<%= @exercise.name %>
</h1>
      <div class="row" >
        <div><%= raw(@exercise.YoutubeLink) %></div>
        <div style="padding-top:160px;"><%= image_tag("#{@exercise.GifLink}", :alt => "#{@exercise.name}", :class=>"col-xs-12 col-sm-6 col-lg-8") %>
</div>
      </div>
      <div class="row">
        <div class="col-xs-12 col-sm-6 col-lg-8"><h3>הוראות:</h3>
<p><%= raw(@exercise.instructions) %></p>
</div>
        <div class="col-xs-12 col-sm-6 col-lg-8"><h3>שרירים מרכזיים שמעורבים:</h3><%= image_tag("#{@exercise.muscles}") %>
</div>
      </div>



  <% if current_user && current_user.admin %>

<%= link_to 'Edit', edit_exercise_path(@exercise) %> |
<%= link_to 'Back', exercises_path %>

<%else%>
<%= link_to 'חזור', exercises_path %>
<%end%>

